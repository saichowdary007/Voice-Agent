syntax = "proto3";

package voiceagent;

message AudioChunk {
  bytes data = 1;
}

message RecognitionResult {
  string text = 1;
  bool is_final = 2;
}

service AudioStream {
  rpc Stream (stream AudioChunk) returns (stream RecognitionResult);
}
